---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: podasgmappings.networking.azure.com
spec:
  group: networking.azure.com
  names:
    kind: PodASGMapping
    listKind: PodASGMappingList
    plural: podasgmappings
    singular: podasgmapping
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Number of mapping rules
      jsonPath: .status.mappingCount
      name: Mappings
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PodASGMapping is the Schema for the podasgmappings API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PodASGMappingSpec defines the desired state of PodASGMapping.
            properties:
              mappings:
                items:
                  description: Mapping maps a pod selector to ASG references.
                  properties:
                    applicationSecurityGroups:
                      items:
                        description: ASGReference references an Azure Application
                          Security Group.
                        properties:
                          description:
                            type: string
                          resourceId:
                            pattern: ^/subscriptions/[^/]+/resourceGroups/[^/]+/providers/Microsoft\.Network/applicationSecurityGroups/[^/]+$
                            type: string
                          subscriptionId:
                            type: string
                        required:
                        - resourceId
                        type: object
                      minItems: 1
                      type: array
                    podSelector:
                      description: PodSelector selects pods by labels.
                      properties:
                        matchLabels:
                          additionalProperties:
                            type: string
                          type: object
                      required:
                      - matchLabels
                      type: object
                  required:
                  - applicationSecurityGroups
                  - podSelector
                  type: object
                minItems: 1
                type: array
            required:
            - mappings
            type: object
          status:
            description: PodASGMappingStatus defines the observed state of PodASGMapping.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              mappingCount:
                type: integer
              mappingStatuses:
                items:
                  description: MappingStatus tracks the status of a single mapping
                    rule.
                  properties:
                    asgSyncState:
                      type: string
                    error:
                      type: string
                    lastSyncTime:
                      format: date-time
                      type: string
                    matchedPods:
                      type: integer
                    selectorHash:
                      type: string
                  required:
                  - asgSyncState
                  - matchedPods
                  - selectorHash
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
